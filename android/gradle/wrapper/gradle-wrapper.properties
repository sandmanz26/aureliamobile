distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-9.3.1-all.zip

# 9.3.1 — the version Flutter 3.47.3's own `flutter create` template ships.
#
# Taken from the template rather than chosen: the Flutter Gradle Plugin
# enforces a minimum, and it moves with the SDK. 8.3 (the 3.27.1 template) was
# rejected for being unable to run on Java 21; 8.7 was then rejected by the
# plugin itself for being below its minimum of 8.14.0. Read the number out of
# `flutter create` rather than guessing at it.
